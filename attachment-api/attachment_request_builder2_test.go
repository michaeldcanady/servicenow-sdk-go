package attachmentapi

import "testing"

// TODO: (TestNewAttachmentRequestBuilder2Internal) Add tests
func TestNewAttachmentRequestBuilder2Internal(t *testing.T) {
	tests := []struct {
		name string
		test func(*testing.T)
	}{}

	for _, test := range tests {
		t.Run(test.name, test.test)
	}
}

// TODO: (TestNewAttachmentRequestBuilder2) Add tests
func TestNewAttachmentRequestBuilder2(t *testing.T) {
	tests := []struct {
		name string
		test func(*testing.T)
	}{}

	for _, test := range tests {
		t.Run(test.name, test.test)
	}
}

// TODO: (TestAttachmentRequestBuilder2_ByID) Add tests
func TestAttachmentRequestBuilder2_ByID(t *testing.T) {
	tests := []struct {
		name string
		test func(*testing.T)
	}{}

	for _, test := range tests {
		t.Run(test.name, test.test)
	}
}

// TODO: (TestAttachmentRequestBuilder2_File) Add tests
func TestAttachmentRequestBuilder2_File(t *testing.T) {
	tests := []struct {
		name string
		test func(*testing.T)
	}{}

	for _, test := range tests {
		t.Run(test.name, test.test)
	}
}

// TODO: (TestAttachmentRequestBuilder2_Upload) Add tests
func TestAttachmentRequestBuilder2_Upload(t *testing.T) {
	tests := []struct {
		name string
		test func(*testing.T)
	}{}

	for _, test := range tests {
		t.Run(test.name, test.test)
	}
}

// TODO: (TestAttachmentRequestBuilder2_Get) Add tests
func TestAttachmentRequestBuilder2_Get(t *testing.T) {
	tests := []struct {
		name string
		test func(*testing.T)
	}{}

	for _, test := range tests {
		t.Run(test.name, test.test)
	}
}

// TODO: (TestAttachmentRequestBuilder2_ToGetRequestInformation) Add tests
func TestAttachmentRequestBuilder2_ToGetRequestInformation(t *testing.T) {
	tests := []struct {
		name string
		test func(*testing.T)
	}{}

	for _, test := range tests {
		t.Run(test.name, test.test)
	}
}
