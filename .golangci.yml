version: "2"
linters:
  enable:
    - dogsled
    - gocognit
    - gosec
    - misspell
    - nakedret
    - staticcheck
    - unconvert
    - unparam
    - whitespace
  exclusions:
    warn-unused: true
    rules:
      - path: _test\.go
        linters:
          - gocyclo
          - errcheck
          - dupl
          - gosec
    paths:
      - docs/assets/snippets/.*\.go$
formatters:
  enable:
    - gofmt
  exclusions:
    warn-unused: true
    generated: lax
    paths:
      - docs/assets/snippets/.*\.go$
run:
  build-tags:
    - preview.query
