version: "2"
linters:
  enable:
    - dogsled
    - gocognit
    - gosec
    - misspell
    - nakedret
    - staticcheck
    - unconvert
    - unparam
    - whitespace
    - gocyclo
    - errcheck
    - dupl
  exclusions:
    warn-unused: true
    rules:
      - path: _test\.go
        linters:
          - gocyclo
          - errcheck
          - dupl
          - gosec
          - typecheck
    paths:
      - docs/snippets/**\.go$
formatters:
  enable:
    - gofmt
  exclusions:
    warn-unused: true
    generated: lax
    paths:
      - docs/snippets/**\.go$
run:
  build-tags:
    - preview.query
