version: "2"
linters:
  enable:
    - dogsled
    - gocognit
    - gosec
    - misspell
    - nakedret
    - staticcheck
    - unconvert
    - unparam
    - whitespace
    - gocyclo
    - errcheck
    - dupl
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - gocyclo
          - errcheck
          - dupl
          - gosec
          - typecheck
      - path: docs/snippets/.*\.go
        linters:
          - all
exclusions:
  generated: lax
  presets:
    - comments
    - common-false-positives
    - legacy
    - std-error-handling
formatters:
  enable:
    - gofmt
run:
  build-tags:
    - preview.query
