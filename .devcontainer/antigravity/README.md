# Google Antigravity CLI Feature

Installs the Google Antigravity CLI (`agy`) into the devcontainer.

## Installation

The installation is performed via the official script:
`curl -fsSL https://antigravity.google/cli/install.sh | bash`

The binary is installed to `/usr/local/bin/agy`.
