{
    "id": "xdg-utils",
    "version": "1.0.0",
    "name": "XDG Utils",
    "description": "Installs xdg-utils and configures a mount for the host browser pipe.",
    "options": {
        "hostBrowserPipePath": {
            "type": "string",
            "default": "/tmp/hostbrowserpipe",
            "description": "Path to the host browser pipe"
        },
        "containerBrowserPipePath": {
            "type": "string",
            "default": "/tmp/hostbrowserpipe",
            "description": "Path to the container browser pipe"
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ],
    "mounts": [
        {
            "source": "/tmp/hostbrowserpipe",
            "target": "/tmp/hostbrowserpipe",
            "type": "bind"
        }
    ]
}