name: Go CI

on:
  workflow_dispatch:
  push:
    branches:
      - "**"
    paths:
      - "**.go"
      - ".github/workflows/go.yml"
  pull_request:
    paths:
      - "**.go"
      - ".github/workflows/go.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

env:
  COVERAGE_FILE: coverage.out

permissions:
  contents: read

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      code: ${{ steps.filter.outputs.any_changed }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Changed Files
        uses: tj-actions/changed-files@v47.0.4
        id: filter
        with:
          files: |
            **.go
            .github/workflows/go.yml

  check-go:
    name: Check Go Modules
    needs:
      - changes
    if: ${{ needs.changes.outputs.code == 'true' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: ["stable", "oldstable"]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go-version }}
      - run: go mod download
      - run: go mod verify
      - run: |
          go mod tidy
          git diff --exit-code -- .

  build-go:
    name: Build Go Code
    needs:
      - changes
    if: ${{ needs.changes.outputs.code == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        go-version: ["stable", "oldstable"]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go-version }}
      - name: Cache Go build
        uses: actions/cache@v5
        with:
          path: ~/.cache/go-build
          key: ${{ runner.os }}-go-build-${{ matrix.os }}-${{ hashFiles('go.sum') }}
          restore-keys: ${{ runner.os }}-go-build-
      - run: go mod download
      - name: Build Go Code (Unix)
        if: runner.os != 'Windows'
        run: |
          DIRS=$(ls -d */ | grep -v docs | sed 's/\(.*\)\//.\/\1\/.../')
          go build . $DIRS
      - name: Build Go Code (Windows)
        if: runner.os == 'Windows'
        run: |
          $dirs = Get-ChildItem -Directory | Where-Object { $_.Name -notmatch "docs|.git|.github" } | ForEach-Object { "./$($_.Name)/..." }
          go build . $dirs

  lint-go:
    name: Lint Go Code
    needs:
      - changes
    if: ${{ needs.changes.outputs.code == 'true' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: ["stable", "oldstable"]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go-version }}
      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: latest
          skip-cache: false
          args: --timeout=5m

  test-go:
    name: Test Go Code
    needs:
      - changes
    if: ${{ needs.changes.outputs.code == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        go-version: ["stable", "oldstable"]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go-version }}
      - name: Cache Go build
        uses: actions/cache@v5
        with:
          path: ~/.cache/go-build
          key: ${{ matrix.os }}-go-build-${{ matrix.go-version }}-${{ hashFiles('go.sum') }}
          restore-keys: ${{ runner.os }}-go-build-${{ hashFiles('go.sum') }}
      - name: Run tests (Unix)
        if: runner.os != 'Windows'
        run: |
          DIRS=$(ls -d */ | grep -v docs | sed 's/\(.*\)\//.\/\1\/.../')
          PACKAGES=$(go list . $DIRS)
          COVERPKG=$(echo "$PACKAGES" | paste -sd "," -)
          go test -count=1 -tags=preview.query -coverpkg="$COVERPKG" -covermode=atomic -coverprofile=$COVERAGE_FILE $PACKAGES
          grep -v -E -f .covignore $COVERAGE_FILE > coverage.filtered.out && mv coverage.filtered.out $COVERAGE_FILE
          go tool cover -func=$COVERAGE_FILE
          go tool cover -html=$COVERAGE_FILE -o cover.html
      - name: Run tests (Windows)
        if: runner.os == 'Windows'
        run: |
          $dirs = Get-ChildItem -Directory | Where-Object { $_.Name -notmatch "docs|.git|.github" } | ForEach-Object { "./$($_.Name)/..." }
          $packages = go list . $dirs
          $coverpkg = $packages -join ","
          go test -count=1 -tags="preview.query" -coverpkg="$coverpkg" -covermode=atomic -coverprofile="$($env:COVERAGE_FILE)" $packages
          dir
          findstr /V /G:.covignore $env:COVERAGE_FILE > coverage.filtered.out
          Move-Item -Path coverage.filtered.out -Destination $env:COVERAGE_FILE -Force
          go tool cover -func="$($env:COVERAGE_FILE)"
          go tool cover -html="$($env:COVERAGE_FILE)" -o cover.html
      - name: Upload Coverage Report
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.os }}-coverage-report-${{ matrix.go-version }}-${{ hashFiles('go.sum') }}) }}
          path: cover.html
      - name: Upload to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
