# Update configuration item

import GoSnippet from '@site/src/components/GoSnippet';
import refGo from '@site/snippets/reference_actions.go';

Replace a CI's attributes.

## HTTP request

```http
PUT /api/now/v1/cmdb/instance/{className}/{sys_id}
```

## Path parameters

| Name | Description |
| ---- | ----------- |
| `className` | Class name |
| `sys_id` | Sys_id |

## Query parameters

None.

## Request body

Supply a `cmdbinstanceapi.CmdbInstance`.

## Response

An item envelope of the updated `CmdbInstance`.

## Example

<GoSnippet language="go" src={refGo} region="ref_update_configuration_item" />
