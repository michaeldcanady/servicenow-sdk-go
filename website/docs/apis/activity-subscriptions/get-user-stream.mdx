# Get user stream

import GoSnippet from '@site/src/components/GoSnippet';
import refGo from '@site/snippets/reference_actions.go';

Retrieve a user's activity stream by profile ID.

## HTTP request

```http
GET /api/now/v1/actsub/userstream/{profileId}
```

## Path parameters

| Name | Description |
| ---- | ----------- |
| `profileId` | Profile ID |

## Query parameters

None.

## Request body

Don't supply a request body for this method.

## Response

An item envelope of `*ActivitySubscriptionModel`.

## Example

<GoSnippet language="go" src={refGo} region="ref_get_user_stream" />
