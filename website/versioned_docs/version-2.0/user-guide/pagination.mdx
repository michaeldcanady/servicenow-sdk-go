# Iterate over every record

import GoSnippet from '@site/src/components/GoSnippet';
import GoExample from '@site/src/components/GoExample';
import paginationGo from '@site/snippets/pagination.go';
import pggGo from '@site/snippets/pagination_guide.go';

Walk a result set that spans multiple pages, letting the SDK follow the
pagination links for you.

## When to use this pattern

- A query can match more records than one response returns, and you need all
  of them
- You're streaming records through a process one at a time rather than
  loading the whole set into memory
- You'd otherwise be hand-assembling `offset`/`limit` loops — the iterator
  works on the [response envelope](../core-concepts.mdx#3-response-envelopes)
  and follows its links instead

## Required values

| Value          | Description                                          |
| -------------- | ---------------------------------------------------- |
| Table name     | The table to read (for example, `incident`)          |
| A first page   | Any list response — its links seed the iterator      |

## Example

<GoExample language="go" files={{ pggGo: pggGo }} template={`package main

{{pggGo:pgg_imports}}

func main() {
    {{pggGo:pgg_main}}
}`} />

## Variations

### Iterate item-by-item

Fetch pages lazily while presenting a record-at-a-time interface:

<GoSnippet language="go" src={paginationGo} region="pagination_item_by_item" />

### Iterate attachments

The Attachment API has its own iterator with the same shape:

<GoSnippet language="go" src={paginationGo} region="pagination_attachment" />

### Iterate in reverse

Pass `true` to walk the result set backwards:

<GoSnippet language="go" src={paginationGo} region="pagination_reverse" />

### Navigate pages manually

Fetch the next page yourself when you need page-level control:

<GoSnippet language="go" src={paginationGo} region="pagination_table_manual" />

### Manage iteration state

<GoSnippet language="go" src={paginationGo} region="pagination_state_management" />

- `Reset()`: Returns the iterator to the first page and first item.
- `ResetPage()`: Restarts iteration of the current page.

## Tips

- Return `false` from the callback to stop early — already-fetched pages
  aren't refetched on a later `Iterate`.
- Combine with a [query](query-builder.mdx) so you page through only the
  records you actually need.

## Next steps

- **[List and query records](tables/list-and-query-records.mdx):** The
  requests that produce paginated results.
- **[Query builder](query-builder.mdx):** Shrink the result set before you
  page through it.
