# Get booking configuration

import GoSnippet from '@site/src/components/GoSnippet';
import refGo from '@site/snippets/reference_actions.go';

Retrieve the appointment-booking configuration for a catalog item.

## HTTP request

```http
GET /api/sn_apptmnt_booking/v1/appointment/configuration
```

## Path parameters

None.

## Query parameters

| Name | Description |
| ---- | ----------- |
| `catalog_id` | Optional — set via the verb's `QueryParameters` type. |

## Request body

Don't supply a request body for this method.

## Response

`ConfigurationResponse` — an item envelope of booking settings.

## Example

<GoSnippet language="go" src={refGo} region="ref_get_configuration" />
